app:
  name: svc
  image:
    repository: "onecx/onecx-demo-svc"
  db:
    enabled: true
  operator:
    keycloak:
      client:
        enabled: true
        spec:
          kcConfig:
            defaultClientScopes: [ ocx-demo:read ]
    microservice:
      spec:
        description: "OneCX backend service"
        name: "onecx-demo-svc"